use html5ever::tokenizer::{
    BufferQueue,
    Tag,
    TagKind,
    TagToken,
    Token,
    TokenSink,
    TokenSinkResult,
    Tokenizer,
    TokenizerOpts,
};

use std::borrow::Borrow;

use url::{
    ParseError,
    Url,
};

#[derive(Default, Debug)]
struct LinkQueue {
    links : Vec<String>,


fn main() {
    println!("Hello, world!");
}
